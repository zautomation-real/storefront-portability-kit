{% if section.settings.text != blank %}
  <div class="announcement">{{ section.settings.text | escape }}</div>
{% endif %}
{% schema %}
{
  "name": "Announcement",
  "settings": [
    { "type": "text", "id": "text", "label": "Text", "default": "__ANNOUNCEMENT__" }
  ]
}
{% endschema %}
